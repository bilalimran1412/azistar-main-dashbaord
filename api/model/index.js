const { UserModel } = require('./User')
const { BotModal } = require('./Bot')
const { GoalModal } = require('./Goal')
const { AuthModal } = require('./Auth')

module.exports = {
  UserModel,
  BotModal,
  GoalModal,
  AuthModal,
}
